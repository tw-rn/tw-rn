{
  "description": "TailwindCSS in React Native",
  "author": "Erick Tamayo",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --stream --parallel --ignore @tw-rn/playground",
    "build": "lerna run build --stream --parallel --ignore @tw-rn/playground --ignore documentation",
    "test": "lerna run test",
    "reset": "find . -type dir -name node_modules | xargs rm -rf && lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  },
  "resolutions": {}
}
